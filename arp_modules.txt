import time, requests



active_devices = {}


for num in range(10):
    if num != 0:
        
    
        device = random.randint(1,267)
        active_devices[num] = f"{device}"

print(active_devices)

choice
